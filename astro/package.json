{
  "name": "filex",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bunx --bun astro dev",
    "build": "bunx --bun astro build --remote",
    "preview": "bun run --bun start",
    "astro": "bunx --bun astro",
    "start": "bun run main.mjs"
  },
  "dependencies": {
    "@astrojs/db": "^0.18.3",
    "@nurodev/astro-bun": "^2.1.2",
    "@types/bun": "^1.3.10",
    "astro": "^5.18.1",
    "bcryptjs": "^3.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^5.9.3"
  }
}
