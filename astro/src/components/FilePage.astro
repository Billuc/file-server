---
import { getFileType } from "@/utils/fileUtils";
import { nullPassword } from "@/utils/passwordUtils";
import FileContent from "./FileContent.astro";
import { t, TranslationKeys } from "@/utils/i18n";

interface Props {
  file: {
    id: string;
    name: string;
    password: string;
  };
}

const { file } = Astro.props;

const hasNoPassword = await nullPassword(file.password);
const fileType = getFileType(file.name);
---

<section class="file-header">
  <h2>{file.name}</h2>
  <span>(<i style="user-select: all">{file.id}</i>)</span>
  {
    !hasNoPassword && (
      <span class="password-protected">
        🔒 {t(TranslationKeys.PasswordProtected, Astro.preferredLocale)}
      </span>
    )
  }

  <div class="spacer"></div>

  <div class="file-buttons">
    {
      fileType === "text" && (
        <button
          id="copy-button"
          class="file-button"
          data-success={t(
            TranslationKeys.ContentCopiedToClipboard,
            Astro.preferredLocale,
          )}
        >
          {t(TranslationKeys.Copy, Astro.preferredLocale)}
        </button>
      )
    }
    <a
      href={`./f/${file.id}?download=true&enc=${encodeURIComponent(
        file.password,
      )}`}
      class="file-button"
    >
      {t(TranslationKeys.Download, Astro.preferredLocale)}
    </a>
    <button
      id="share-button"
      class="file-button"
      data-success={t(
        TranslationKeys.UrlCopiedToClipboard,
        Astro.preferredLocale,
      )}>{t(TranslationKeys.Share, Astro.preferredLocale)}</button
    >
  </div>
</section>

<FileContent file={file} fileType={fileType} />

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const copyButton = document.getElementById("copy-button")!;
    const shareButton = document.getElementById("share-button")!;
    const fileContent = document.getElementById("file-content")!;

    if (copyButton) {
      const copyContent = () => {
        navigator.clipboard.writeText(fileContent.textContent).then(() => {
          alert(copyButton.dataset.success || "Copié");
        });
      };
      copyButton.addEventListener("click", copyContent);
    }

    const shareContent = () => {
      navigator.clipboard.writeText(window.location.href).then(() => {
        alert(shareButton.dataset.success || "Copié");
      });
    };

    shareButton.addEventListener("click", shareContent);
  });
</script>
